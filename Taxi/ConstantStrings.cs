namespace TaxiStation
{
    internal static class ConstantStrings
    {
        public static readonly string EmployeeSalary = "Employee's salary can't be 0 or less.";
        public static readonly string NotWhiteSpaceOrDigit = "Name can't be a whitespace and must contatin only digits.";
        public static readonly string NegativeNumbers = "No negative numbers allowed.";
        public static readonly string NoDrivers = "No drivers are working in TaxiStation.";
        public static readonly string NoCustomer = "No customer found with such ID";
    }
}
